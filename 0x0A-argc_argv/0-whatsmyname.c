#include <stdio.h>

/**
 * main - prints program name, followed by a new line
 * @argc: int counts the arguments passed to main
 * @argv: array of passed elements(arguments)
 * Return: 0 (suceess)
 */

int main(int argc, char const *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

